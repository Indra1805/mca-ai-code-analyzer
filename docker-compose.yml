services:

  web:

    build: .

    container_name: ai_code_analyzer

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media

volumes:

  static_volume:

  media_volume: